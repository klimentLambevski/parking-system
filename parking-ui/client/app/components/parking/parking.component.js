import template from './parking.html';
import controller from './parking.controller';

let parkingComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller,
    controllerAs: 'vm'
};

export default parkingComponent;